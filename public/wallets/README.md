# Wallet Brand Assets

- `tokenpocket.svg`, `imtoken.svg`, `bitget.svg`, and `okx.svg` are branded SVG assets extracted from `@web3icons/core` 4.0.53 (MIT).
- `tronlink.ico` is downloaded from the public TronLink website favicon.
